{
  "correlation_results": [
    {
      "runtime_event": "HTTP GET 127.0.0.1:5233/api/health",
      "ida_evidence": "No direct IDA xref found for /api/health path. The path is likely constructed dynamically or loaded from configuration.",
      "confidence": "UNRESOLVED",
      "note": "The health check endpoint is not among the target strings analyzed in IDA. It may be part of a UE4 HTTP module or plugin."
    },
    {
      "runtime_event": "HTTP OPTIONS 127.0.0.1:5233/api/health",
      "ida_evidence": "CORS preflight suggests browser-like HTTP client or UE4 HTTP module with CORS support.",
      "confidence": "UNRESOLVED",
      "note": "The OPTIONS method is not among the G20 target strings."
    },
    {
      "runtime_event": "TCP connection attempts to 127.0.0.1:1042 (reset)",
      "ida_evidence": "Port 1042 is not among the analyzed strings. May be a local service or pipe.",
      "confidence": "UNRESOLVED",
      "note": "Repeated connection attempts suggest a service discovery or named pipe alternative."
    },
    {
      "runtime_event": "No external HTTP requests during capture",
      "ida_evidence": "WinHttp and WinInet strings found in binary. External connection to 43.129.138.3:80 was established before capture.",
      "confidence": "STATIC_CONFIRMED_RUNTIME_NOT_OBSERVED",
      "note": "The external HTTP connection was already established before the capture window. The game may have completed its startup HTTP sequence before our observation."
    },
    {
      "runtime_event": "No DNS queries during capture",
      "ida_evidence": "No DNS-related strings were analyzed in IDA.",
      "confidence": "UNRELATED",
      "note": "DNS resolution likely occurred before capture started."
    }
  ],
  "classification": "RUNTIME_PATH_OBSERVED_CONTRACT_INCOMPLETE",
  "note": "Runtime observation confirmed localhost HTTP health checks but did not capture the startup HTTP sequence. The external connection was established before capture started. IDA analysis confirmed the presence of HTTP-related strings but found no direct xrefs (UE4 indirect reference pattern)."
}
